

{% extends "_includes/columnWrap" %}
{% block content %}
	{% include '_includes/field-label' with {item:item} %}
    <input {% if item.required %}required{% endif %} id="{{ item.fieldName|lower|replace(' ', '-') }}" type="{{ item.fieldType }}" name="message[{{ item.fieldName }}]" value="">
{% endblock %}